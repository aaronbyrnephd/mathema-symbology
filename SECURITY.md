# Security policy

Report a suspected vulnerability privately to
contact@aaronbyrnephd.com rather than opening a public issue. You'll
get an acknowledgement within 10 days and a fix or a considered
response within 30.

This package reads one local YAML file (`.mathema/symbology.yaml`,
parsed with `yaml.safe_load`) and takes no network or subprocess
action, so the realistic surface is small.